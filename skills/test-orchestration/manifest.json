{
  "name": "test-orchestration",
  "version": "1.0.0",
  "description": "Full real-world orchestration test: Creates a test mission, spins up TMux session with deputy + IMPs, assigns real work, monitors progress, validates completion, and generates comprehensive report",
  "author": "ORC Development Team",
  "tags": ["testing", "orchestration", "integration", "tmux", "multi-agent"],
  "parameters": [
    {
      "name": "mission_prefix",
      "description": "Prefix for test mission ID",
      "type": "string",
      "default": "MISSION-TEST-ORC"
    },
    {
      "name": "grove_name_prefix",
      "description": "Prefix for test grove name",
      "type": "string",
      "default": "test-canary"
    },
    {
      "name": "timeout_minutes",
      "description": "Maximum time to wait for implementation",
      "type": "number",
      "default": 30
    },
    {
      "name": "cleanup_on_success",
      "description": "Clean up test environment after successful run",
      "type": "boolean",
      "default": true
    },
    {
      "name": "cleanup_on_failure",
      "description": "Clean up test environment after failed run",
      "type": "boolean",
      "default": false
    }
  ]
}
